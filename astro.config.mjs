// @ts-check
import { defineConfig } from 'astro/config';

export default defineConfig({
  // 본인의 깃허브 아이디를 넣으세요
  site: 'https://crbac3217.github.io',
  // 깃허브 레포지토리 이름과 똑같이 적어주세요 (슬래시 포함)
  base: '/Pfweb', 
});